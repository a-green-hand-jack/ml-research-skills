interface:
  display_name: "Reference Library Manager"
  short_description: "Index and monitor project reference sources."
  default_prompt: "Scan the project reference folder, update the source index and processing status, and report new papers, docs, bundles, duplicates, metadata gaps, and next reading actions."
