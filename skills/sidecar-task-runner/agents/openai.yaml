interface:
  display_name: "Sidecar Task Runner"
  short_description: "Run bounded one-shot Codex sidecar tasks, including precommit and personalization scans."
  default_prompt: "Create a sidecar task bundle, run a one-shot Codex Spark sidecar if useful, and record the output under .agent/sidecars/. For commit closeout, use the precommit-classifier preset; for preference extraction, use the personalization-scanner preset."
